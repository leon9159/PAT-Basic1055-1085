#include <stdio.h>
int main()
{
  int m,n,a,b,t;
  scanf("%d %d %d %d %d",&m,&n,&a,&b,&t);
  int i,j,r;
  for(i=0;i<m;i++)
  {
    for(j=0;j<n;j++)
    {
      scanf("%d",&r);
      if(r<=b &&r>=a)
        r=t;
      if(j==0)
      printf("%03d",r);
      else
      printf(" %03d",r);
    }
    printf("\n");
  }
  return 0;
}
