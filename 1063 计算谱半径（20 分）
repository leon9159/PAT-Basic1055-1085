#include<stdio.h>
#include<math.h>
int main()
{
  int n;
  scanf("%d",&n);
  int i,x,y;
  float num,max=0;
  for(i=0;i<n;i++)
  {
    scanf("%d %d",&x,&y);
    num=sqrt(pow(x,2)+pow(y,2));
    if(num>max)
     max=num;
  }
  printf("%.02f",max);
  return 0;
}
